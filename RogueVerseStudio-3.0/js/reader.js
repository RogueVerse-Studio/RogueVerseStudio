const pages=[...document.querySelectorAll('.manga-page')];const progress=document.querySelector('#progress');function update(){let best=0,min=Infinity;pages.forEach((p,i)=>{const d=Math.abs(p.getBoundingClientRect().top-150);if(d<min){min=d;best=i}});progress.textContent=`Page ${best+1} of ${pages.length}`;localStorage.setItem('rvs-sentou-issue-1',String(best+1))}addEventListener('scroll',update,{passive:true});update();document.querySelector('#fullscreen')?.addEventListener('click',()=>document.documentElement.requestFullscreen?.());
